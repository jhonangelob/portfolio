import { Experience, Technology } from '@/utils/types';

export type Props = {
  experiences: Experience;
  technologies: Technology;
  textContent: {
    subText1: string;
    subText2: string;
  };
};
